# Proxy configuration for the macro-analysis collector.
# Copy to macro_proxy.env and fill in, or pass --proxy-url / set MACRO_PROXY_URL.
# Only HTTP/HTTPS proxies are supported; SOCKS is rejected.

MACRO_PROXY_URL=http://127.0.0.1:10809
